# VERIFY — what still needs eyes on it in Logic

Everything in `CONFIRMED` in `validate.py` has been checked against Apple's guide or
two independent references. This file is the remainder: claims that are almost
certainly right but that nobody has watched happen on a real machine.

Open Logic, work down the list, and tick as you go. Anything that fails, say so and
it gets fixed at the source and the book regenerated.

## Key commands not yet documented (23)

Check each in **Logic Pro > Key Commands (⌥K)** — search the command name; the
assigned key shows on the right.

- [ ] `C` — S4 “Record into a cycle with take folders”
- [ ] `E` — S11 “Create a Pattern Region to open the Step Sequencer”
- [ ] `G` — S2 “Use the Arrangement track to move whole sections”; S18 “Show the Chord track in Global Tracks”
- [ ] `L` — S2 “Loop a region instead of copying it”
- [ ] `M` — S2 “Mute regions instead of deleting them while experimenting”
- [ ] `R` — S6 “Punch in on the fly with R”
- [ ] `U` — S2 “Snap arrangement markers to your actual section boundaries”; S2 “Solo an arrangement section with the locators”; S3 “Use the Cycle area as a bookmark”; S9 “Set the locators to the marquee instantly”
- [ ] `X` — S13 “Every pad is its own channel strip”; S15 “X toggles the Mixer, and it remembers your scroll position”
- [ ] `Z` — S2 “Zoom all the way out and squint”; S3 “Z is the only zoom command you need”
- [ ] `⌘` — S7 “⌘ is your secondary tool”
- [ ] `⌘,` — S1 “Run two buffer sizes, not one”
- [ ] `⌘.` — S6 “⌘. discards the take you just recorded”
- [ ] `⌘B` — S17 “⌘B bounces the project or the current section”
- [ ] `⌘J` — S7 “⌘J joins regions back into one”
- [ ] `⌘R` — S2 “Repeat regions numerically instead of dragging”; S7 “⌘R repeats the selection once, instantly”
- [ ] `⌘S` — S1 “Set autosave and then still hit ⌘S”
- [ ] `Esc` — S7 “Esc opens the tool menu, Esc twice returns to the pointer”
- [ ] `Space` — S9 “Press Space and Logic plays only the marquee selection”
- [ ] `Delete` — S9 “Hit Delete to cut a hole in every track at once”
- [ ] `⇧ + click` — S9 “Hold ⇧ to extend an existing marquee”
- [ ] `⌃⌥ + drag` — S3 “Drag‑zoom to any area with ⌃⌥”
- [ ] `⌥ + click` — S11 “⌥‑click a step to reset it to default”; S15 “⌥‑click any control to reset it to default”; S15 “⌥-click Solo for exclusive solo”; S15 “Bypass with ⌥-click, browse with the slot arrows”
- [ ] `⌥K to assign` — S1 “Turn on Low Latency Mode and give it a key command”; S3 “Know what the number keys actually do”; S3 “Assign a key to “Play Selection””; S3 “Toggle Catch to stop the screen running away”; S7 “Trim to the playhead from either direction”; S8 “Put take-switching on keys you choose”

## Menu paths (6)

Walk each menu and confirm the wording matches your version.

- [ ] `File > Project Settings` — S1
- [ ] `Logic Pro > Settings > Audio` — S1
- [ ] `Logic Pro > Settings > Display` — S1
- [ ] `Logic Pro > Settings > General` — S1
- [ ] `Logic Pro > Settings > MIDI` — S1
- [ ] `Logic Pro > Settings > Recording` — S1
